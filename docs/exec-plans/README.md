# Execution Plans

Execution plans are first-class repo artifacts for work that needs more context
than a short prompt.

- Keep active plans in `active/`.
- Move completed or abandoned plans to `completed/` with a final status note.
- Use `docs/agent-harness/exec-plan-template.md` as the starting point.

Small, low-risk edits do not need a committed plan.
